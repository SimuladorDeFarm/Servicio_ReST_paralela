fastapi
uvicorn[standard]
pydantic-settings
python-dotenv
# Carga paralela y estructura en memoria (data_loader / data_store).
# Versiones fijadas por compatibilidad con Python 3.8.
numpy==1.24.4
pandas==1.5.3
# Logging (rotación/retención de archivos manejada por la librería)
loguru==0.7.2
# Pruebas
pytest==7.4.4
